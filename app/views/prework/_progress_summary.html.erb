    <h4>Overall Progress: <span id="text_overallprogress"><%= @average %>%</span> </h4>
    <span id="refresh"><%= link_to "Update Progress", update_progress_path  %></span>    
    <div class="progress progress-info progress-striped active">
      <div class="bar" id="overallprogress" style="width: <%= @average %>%" >
      </div>
    </div>
<div id="highlight-sidebar">  
      <ul class="nav nav-list">
  
  <% last_subject = nil %>
  <% @user_courses.each do |subject, courses| %>
    <li><%= link_to subject, '#' + subject.downcase.gsub(/\s+/, '').gsub('&', "").gsub(':', "") %></li>
  <% end %>
</div>
  </ul>